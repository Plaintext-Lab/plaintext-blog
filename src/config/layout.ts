// Layout budgets shared by every page. Structural widths are the one place
// raw pixel values belong; everything inside uses Astryx tokens.
export const PROSE_WIDTH = 720;

// Outer page padding on the Astryx spacing scale (6 = 24px).
export const PAGE_PADDING = 6;
